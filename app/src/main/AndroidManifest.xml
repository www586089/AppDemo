<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppDemo"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

        <activity android:name=".activity.matrix.BasicUseActivity" />
        <activity android:name=".activity.matrix.TranslateActivity" />
        <activity android:name=".activity.matrix.RotateActivity" />
        <activity android:name=".activity.matrix.ScaleActivity" />
        <activity android:name=".activity.matrix.SkewActivity" />
        <activity android:name=".activity.matrix.ClipActivity" />
        <activity android:name=".activity.matrix.InvertActivity" />
        <activity android:name=".activity.matrix.MapActivity" />
        <activity android:name=".activity.MatrixActivity" />
        <activity android:name=".activity.PathEffectActivity" />
        <activity android:name=".activity.ColorFilterActivity" />
    </application>

</manifest>